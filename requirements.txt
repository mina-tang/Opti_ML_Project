# torch, torchvision (required, but not in requirements in order to let user choose between cpu and gpu version of pytorch)
torch~=2.3.0+cu121
torchvision ~=
# SPSA from qiskit_algorithms.optimizers for the executing SPSA algorithm
numpy
jupyter
datasets
tqdm
ucimlrepo
pandas
scikit-learn